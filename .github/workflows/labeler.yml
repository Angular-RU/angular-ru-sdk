name: Pull Request Labeler
on: [push]

jobs:
    triage:
        name: triage
        runs-on: ubuntu-latest
        steps:
            - uses: actions/labeler@v3
              with:
                  repo-token: ${{ secrets.GH_TOKEN }}
