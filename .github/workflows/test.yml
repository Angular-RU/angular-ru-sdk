name: CI Test
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - name: Scan repo for find local actions
        uses: actions/checkout@v2

      - name: Setup Node.js and Cache
        uses: ./.github/actions/nodejs

      - name: Run build artifacts dependent
        run: |
          npx nx build cdk
          npx nx build jest
          npx nx build eslint-plugin-enterprise

      - name: Run unit tests
        run:
          npx nx run-many --target=test --parallel=1 --all --collect-coverage --exclude $(nx affected:apps --base=master
          --plain)

      - uses: codecov/codecov-action@v2
        with:
          directory: ./coverage/cdk/
          flags: summary,cdk
          name: cdk

      - uses: codecov/codecov-action@v2
        with:
          directory: ./coverage/ngxs/
          flags: summary,ngxs
          name: ngxs

      - uses: codecov/codecov-action@v2
        with:
          directory: ./coverage/jest/
          flags: summary,jest
          name: jest

concurrency:
  group: test-${{ github.head_ref }}
  cancel-in-progress: true
